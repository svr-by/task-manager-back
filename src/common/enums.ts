export enum MODEL_NAME {
  USER = 'User',
  PROJECT = 'Project',
  COLUMN = 'Column',
  TASK = 'Task',
}

export enum COLLECTIONS {
  USERS = 'users',
  PROJECTS = 'projects',
  COLUMNS = 'columns',
  TASKS = 'tasks',
}
