export enum MODEL_NAME {
  USER = 'User',
  COMPANY = 'Company',
}

export enum COLLECTIONS {
  USERS = 'users',
  COMPANIES = 'companies',
  STORAGES = 'storages',
  PRODUCTS = 'products',
  STOCKS = 'stocks',
  SHIFTS = 'shifts',
  ENTRIES = 'entries',
}
